import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#36558f',
        },
        secondary: {
            main: '#40376e',
        },
        info: {
            main: '#ffffff',
        },
    }
})

export default theme;